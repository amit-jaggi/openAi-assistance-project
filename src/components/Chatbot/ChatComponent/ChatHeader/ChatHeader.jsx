import './ChatHeader.scss';

export default function ChatHeader() {
  return (
    <div className={`chat-header-container`}>
      <main className={`chat-title`}>React + OpenAI Chatbot</main>
    </div>
  );
};
